#pragma once
#include <math.h>

static const float gFriction = 0.7f;
static const int WINDOWWIDTH = 2880;
static const int WINDOWHEIGHT = 1800;
static const float WINDOWCORNERTOCORNER = 10000; // sqrtf(WINDOWWIDTH*WINDOWWIDTH + WINDOWHEIGHT*WINDOWHEIGHT);